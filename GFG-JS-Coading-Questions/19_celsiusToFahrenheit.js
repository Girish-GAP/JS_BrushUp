// Write a Program to convert Celsius to Fahrenheit in JavaScript?

// formula : F = (c * 9/5 ) + 32

const fehrenheitToCelsius = (celsius) => {
  return celsius * (9 / 5) + 32;
};

console.log(fehrenheitToCelsius(20));
