// Calculate the Power of a Number in JavaScript?

const calPower = (num, pow) => {
  // let ans = 1;
  // for(let i = 1; i <= pow; i++){
  //     ans = ans * num;
  // }
  // return ans;

  // OR
  return num ** pow;
};

console.log(calPower(3, 4));
